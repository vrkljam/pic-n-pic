const videoElement =document.getElementById('video');
const button =document.getElementById('button');

// select media stream, pass video to element then play

async function selectMediaStream(){
    try {

    }
    catch (error){
       console.log('oops', error)
    }
}

selectMediaStream ();
